{
  "_comment_Jwt_Secret": "This is a placeholder only, not a real secret. For local development, override Jwt:Secret with a real random value via 'dotnet user-secrets set \"Jwt:Secret\" \"<value>\"' run from src/SpareParts.Api. Never commit a real secret to this file.",
  "Jwt": {
    "Secret": "DEV_ONLY_PLACEHOLDER_SET_VIA_USER_SECRETS_MIN_32_CHARS",
    "Issuer": "SpareParts.Api",
    "Audience": "SpareParts.Desktop",
    "ExpiryHours": 12
  }
}
