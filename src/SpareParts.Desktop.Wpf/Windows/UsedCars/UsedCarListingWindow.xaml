<Window x:Class="SpareParts.Desktop.Wpf.UsedCarListingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Listing Package"
        Width="640"
        Height="640"
        MinWidth="520"
        MinHeight="480"
        Background="{DynamicResource AppBackgroundBrush}"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style x:Key="ListingActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="14 0"/>
            <Setter Property="Margin" Value="0 0 8 0"/>
            <Setter Property="Background" Value="{DynamicResource CardHighlightBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="ListingLabelStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="0 12 0 4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
        </Style>
        <Style x:Key="ListingTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="One-tap listing package — copy the text below and paste it into the marketplace's create-listing page, then attach the photos from this car's gallery."
                   TextWrapping="Wrap" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>

        <TextBlock Grid.Row="1" Text="Title" Style="{StaticResource ListingLabelStyle}"/>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TitleTextBox" Grid.Column="0" Style="{StaticResource ListingTextBoxStyle}" Height="44" AcceptsReturn="False"/>
            <Button Grid.Column="1" Style="{StaticResource ListingActionButtonStyle}" Margin="8 0 0 0" Content="Copy" Click="OnCopyTitleClick"/>
        </Grid>

        <TextBlock Grid.Row="3" Text="Description" Style="{StaticResource ListingLabelStyle}" VerticalAlignment="Top"/>
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="DescriptionTextBox" Grid.Column="0" Style="{StaticResource ListingTextBoxStyle}" Height="220"/>
            <Button Grid.Column="1" Style="{StaticResource ListingActionButtonStyle}" Margin="8 0 0 0" VerticalAlignment="Top" Content="Copy" Click="OnCopyDescriptionClick"/>
        </Grid>

        <TextBlock x:Name="PhotoCountText" Grid.Row="5" Margin="0 12 0 0" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" Margin="0 16 0 0">
            <TextBlock Text="Open marketplace:" VerticalAlignment="Center" Margin="0 0 10 0" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
            <ItemsControl x:Name="MarketplaceLinkscontrol">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource ListingActionButtonStyle}" Content="{Binding Name}" Click="OnOpenMarketplaceClick" Tag="{Binding Url}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</Window>
